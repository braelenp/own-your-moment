{
  "name": "own-your-moment-workspace",
  "private": true,
  "scripts": {
    "anchor:build": "anchor build",
    "anchor:test": "anchor test",
    "app:dev": "yarn --cwd app dev",
    "app:build": "yarn --cwd app build"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@types/bn.js": "^5.1.5",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^4.5.0",
    "ts-mocha": "^10.1.0",
    "typescript": "^5.7.3"
  }
}
